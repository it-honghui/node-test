{
  "name": "cryptopunks",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ it-whh/cryptopunks",
    "create-local": "graph create --node http://localhost:8020/ it-whh/cryptopunks",
    "remove-local": "graph remove --node http://localhost:8020/ it-whh/cryptopunks",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 it-whh/cryptopunks",
    "test": "graph build && graph test CryptoPunks",
    "test:force": "rm -rf ./tests/.bin ./build && yarn test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.28.0",
    "@graphprotocol/graph-ts": "0.26.0",
    "keytar": "7.4.0"
  },
  "devDependencies": {
    "matchstick-as": "0.2.3"
  }
}
