{
  "name": "opensea",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ opensea-subgraph",
    "create-local": "graph create --node http://localhost:8020/ opensea-subgraph",
    "remove-local": "graph remove --node http://localhost:8020/ opensea-subgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 opensea-subgraph"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.28.0",
    "@graphprotocol/graph-ts": "0.26.0",
    "keytar": "^7.7.0"
  }
}
